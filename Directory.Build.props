<Project>
  <!-- Shared defaults for all projects. Individual projects may override
       (e.g. the test project sets LangVersion to latest). -->
  <PropertyGroup>
    <Version>1.1.2</Version>
    <LangVersion>7.3</LangVersion>
  </PropertyGroup>
</Project>
